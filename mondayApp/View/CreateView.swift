import SwiftUI

struct createView: View {
    
    @EnvironmentObject var appState: AppState
    
 
    
    var body: some View {
        ZStack{
            Text("create").foregroundColor(.white)
        }
    }
}

